{% extends "EshopBundle::layout.html.twig" %}

{% block body %}

    {# On définit un sous-titre commun à toutes les pages du bundle, par exemple #}
    <h1>{{ product.title }}</h1>

    <p>{{ product.content }}</p>

<p>{{ product.price }}</p>

{% endblock %}